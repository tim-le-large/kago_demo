name: kago
description: KaGo — Flutter demo for KVV stops, departures, and trips (TRIAS via Worker).
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.0
  equatable: ^2.0.7
  http: ^1.3.0
  intl: ^0.20.0
  bloc_concurrency: ^0.3.0
  shared_preferences: ^2.3.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
