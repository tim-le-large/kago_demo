name: Deploy frontend (GitHub Pages)

on:
  push:
    branches: [main]
    paths:
      - "lib/**"
      - "web/**"
      - "android/**"
      - "ios/**"
      - "macos/**"
      - "windows/**"
      - "linux/**"
      - "test/**"
      - "pubspec.yaml"
      - "pubspec.lock"
      - "analysis_options.yaml"
      - ".github/workflows/deploy-frontend-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Build web
        env:
          API_BASE_URL: https://api.kago.lelar.ge
        run: |
          flutter build web --release \
            --dart-define=API_BASE_URL=$API_BASE_URL \
            --base-href /

      - name: Configure custom domain
        run: echo "kago.lelar.ge" > build/web/CNAME

      - uses: actions/upload-pages-artifact@v3
        with:
          path: build/web

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4

